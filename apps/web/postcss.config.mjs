// Tailwind 4 is configured CSS-first in app/globals.css via @theme.
// There is deliberately no tailwind.config.js. See DESIGN.md.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
